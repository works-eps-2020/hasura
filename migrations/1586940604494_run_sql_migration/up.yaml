- args:
    cascade: false
    read_only: false
    sql: "CREATE OR REPLACE VIEW \"user_typing\" AS \n SELECT chat_id,\n    user_id,\n
      \   last_typed\n   FROM chat_user\n  WHERE (last_typed > (now() - '00:00:02'::interval));"
  type: run_sql
- args:
    name: user_typing
    schema: public
  type: add_existing_table_or_view
